[Comments]
# !ln -s %:p $HOME/.config/mimeapps.list
# Run `xdg-mime query filetype <filename>` to determine the MIME type of the file
# Run `xdg-mime query default <mimetype>` to determine the default application for the MIME type
# To find the appropriate .desktop file for the application are typically stored at `/usr/share/applications/`. Accordingly, you can run `grep -l <application> /usr/share/applications/*.desktop` to find the appropriate .desktop file.
# `/usr/share/applications/mimeinfo.cache` stores the associations between MIME types and applications. It is generated by `update-desktop-database`.

[Added Associations]
application/pdf=okularApplication_pdf.desktop;

[Removed Associations]

[Default Applications]
application/pdf=org.pwmt.zathura.desktop
text/html=brave-browser.desktop
text/plain=nvim.desktop
inode/directory=yazi.desktop
x-scheme-handler/http=brave-browser.desktop
x-scheme-handler/https=brave-browser.desktop
x-scheme-handler/about=brave-browser.desktop
x-scheme-handler/unknown=brave-browser.desktop
x-scheme-handler/mailto=brave-browser.desktop
